@using scheapp.app.Models.API
@model ProfessionalServiceRS

@{
    Layout = "_AdminLayout";
}
<div class="card p-5">
    <h1>Professionals</h1>
    @await Html.PartialAsync("_ProfessionalListTable", new RequestByBusinessId { BusinessId = Model.BusinessId.GetValueOrDefault() })
</div>
